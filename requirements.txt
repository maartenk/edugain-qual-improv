# Development dependencies (runtime + dev tools)
# Auto-generated from pyproject.toml
# Install with: pip install -r requirements.txt

# Runtime dependencies
requests>=2.28.0
platformdirs>=3.0.0

# Development dependencies
pytest>=7.4.0
ruff>=0.1.0
pre-commit>=3.4.0

# Optional extras (install on demand)
# Coverage reporting: pip install -e .[coverage]
# Parallel testing:   pip install -e .[parallel]
# Full test tooling:  pip install -e .[tests]
